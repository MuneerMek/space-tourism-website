import { Link, Route, Routes } from "react-router-dom";

const Destination = () => {
  return (
    <main className="container">
      <p>Destination</p>
    </main>
  );
};
export default Destination;
